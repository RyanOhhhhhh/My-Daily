2026-05-11 开发前的要求
我是开发小白，我现在想开发一个日常生活记录软件，这个软件可以先做网页版，后续再做安卓版本。 先说下我想开发的一些需求：
一、基础模块： 
1.有登录界面，并且对登录进行一个验证。 
2.生活记录界面，最好是像markdown一样，可以记录日常的文字，同时可以上传照片，照片最好是能和文字在一起呈现，并且要有时间和地点记录 
3.时间线界面，之前的日常记录通过时间线可以进行一个排序，有年月日的时间线，并且可以记录哪天记录了，哪天没记录，最好使用记录的标题来呈现，点击标题会跳转到详细的记录。 
4.地图记录界面，之前的日常记录，通过地图的方式呈现，靠之前的记录的地点在地图上做好标记，并且通过上传的照片在地图上呈现，免费最好是在线地图。
二、功能要求：
1.界面要尽量简约美观
2.前后端分离，前端负责记录和计算，后端负责存储数据
3.存储方面的话，我想着云端存储比较方便，在哪都可以，但是最好是先拿本地存储试一下，之后想办法在云服务器和本地nas之间都进行数据存储
4.以后生成了什么，做了什么，都在开发日志.md里面做好记录以及时间，方便后续迭代。
先别进行开发，在开发前先询问下我的意见

---

## 2026-05-11 开发框架总纲

### 一、整体架构

```
┌─────────────────────────────────────────────────┐
│                   前端 (Web)                       │
│  React / Vue + 路由 + 状态管理 + HTTP 请求        │
└────────────────────┬────────────────────────────┘
                     │ API 通信
┌────────────────────▼────────────────────────────┐
│                后端 (API Server)                   │
│  Node.js / Python + RESTful API                  │
└────────────────────┬────────────────────────────┘
                     │ 数据读写
┌────────────────────▼────────────────────────────┐
│                  数据层                           │
│  本地: SQLite / JSON 文件                        │
│  云端: MySQL / PostgreSQL                        │
│  NAS : 后续扩展                                   │
└─────────────────────────────────────────────────┘
```

### 二、推荐技术栈

| 层级 | 推荐方案 | 说明 |
|------|----------|------|
| 前端框架 | ✅ Vue 3 | |
| UI 组件库 | Element Plus / Naive UI | 简约美观 |
| 地图 | Leaflet + OpenStreetMap | 免费在线地图，轻量 |
| 富文本编辑 | 自建 Markdown 编辑器 | 类 Markdown 写作体验 |
| 后端框架 | ✅ Python (FastAPI) | 轻量快速，自动生成 API 文档 |
| 本地数据库 | ✅ SQLite | 无需服务，文件即库 |
| 云端数据库 | MySQL / PostgreSQL | 后续迁移 |
| 图片存储 | 本地文件 → 后续 OSS / NAS | |

### 三、模块划分

#### 1. 用户模块
- 注册 / 登录 / 登出
- 登录态验证（JWT Token）
- 用户信息管理

#### 2. 日记记录模块
- 创建 / 编辑 / 删除日记
- Markdown 编辑器（标题 + 正文）
- 图片上传，文中穿插展示
- 自动记录时间 + 地点（IP 定位 / 手动选择）
- 日记详情页

#### 3. 时间线模块
- 按年 → 月 → 日 三级导航
- 日历视图，标记有记录的日子
- 显示记录标题列表
- 点击标题跳转详情

#### 4. 地图模块
- Leaflet + OpenStreetMap 在线地图
- 有地点的记录在地图打标
- 点击标记显示标题 + 照片缩略图
- 点击跳转详情

### 四、前后端 API 概要

```
POST   /api/auth/register     注册
POST   /api/auth/login        登录
GET    /api/records           获取日记列表（支持时间筛选）
POST   /api/records           创建日记
GET    /api/records/:id       获取单条日记
PUT    /api/records/:id       编辑日记
DELETE /api/records/:id       删除日记
POST   /api/upload            上传图片
GET    /api/records/timeline  获取时间线数据
GET    /api/records/map       获取地图标记数据
```

### 五、开发路线图

| 阶段 | 内容 | 说明 |
|------|------|------|
| Phase 1 | 基础框架搭建 | 前后端脚手架、建表、登录注册 |
| Phase 2 | 日记记录核心功能 | Markdown 编辑器、图片上传、时间地点 |
| Phase 3 | 时间线视图 | 日历导航、日期标记、标题列表 |
| Phase 4 | 地图视图 | 地图集成、定位标记 |
| Phase 5 | UI 美化与细节打磨 | |
| Phase 6 | 本地转云端存储 | 数据库迁移、服务器部署 |
| Phase 7 | Android 版本 | WebView 或原生重写 |

### 六、数据表设计（概要）

**users 表**：id, 用户名, 密码(加密), 昵称, 创建时间

**records 表**：id, 用户id, 标题, 正文(markdown), 记录时间, 地点名称, 经度, 纬度, 创建时间, 更新时间

**photos 表**：id, 记录id, 图片路径/URL, 上传时间

---

*以上为框架总纲，后续各阶段细化具体实现。*

---

## 2026-05-11 Phase 1：前端脚手架搭建完成

已完成：
- 使用 Vite 创建 Vue 3 项目（`frontend/`）
- 安装 vue-router@4
- 开发服务器 `http://localhost:5173/` 运行正常

下一步：
- 安装 UI 组件库（Element Plus / Naive UI 待选）
- 搭建前端路由框架（登录页、首页等）
- 搭建后端 Python FastAPI 项目

---

## 2026-05-11 Phase 1：前端核心页面框架搭建完成

已完成：
- 创建 `src/router/index.js` — 路由配置，含登录守卫
- 创建 `src/views/Login.vue` — 登录页
- 创建 `src/views/MainLayout.vue` — 主布局（侧边栏导航 + 内容区）
- 创建 `src/views/Timeline.vue` — 时间线页面（年导航 + 月网格 + 记录列表）
- 创建 `src/views/MapView.vue` — 地图页面（占位，后续集成 Leaflet）
- 创建 `src/views/RecordEdit.vue` — 记录编辑页（标题 + 地点 + Markdown 正文 + 照片）
- 创建 `src/views/RecordDetail.vue` — 记录详情页

当前页面路由：
| 路由 | 页面 |
|------|------|
| `/login` | 登录页 |
| `/timeline` | 时间线（首页默认） |
| `/map` | 地图视图 |
| `/record/new` | 新建记录 |
| `/record/:id` | 记录详情 |
| `/record/:id/edit` | 编辑记录 |

数据目前为本地 Mock，后续对接后端 API。


## 2026-05-11 Phase 1：git了一下
创建了git项目My-DIARY

---

## 2026-05-11 Phase 1：时间线模块重构 — 纵向时间线 + 折叠效果

### 改动内容
重写 [Timeline.vue](src/views/Timeline.vue)，将原来的年/月/月历卡片式布局改为**纵向时间线**布局：

- **纵向时间线**：左侧一条竖线贯穿，年份、月份、日记条目沿线排列
- **折叠展开**：
  - 年份节点可折叠/展开其月份列表
  - 月份节点可折叠/展开其日记列表
  - 折叠状态通过圆点填充状态和箭头方向直观展示
- **视觉层次**：
  - 年份：大实心圆（28px）+ 加粗大字
  - 月份：中小实心圆（16px）+ 中等字号
  - 日记：小圆点（10px）+ 卡片样式，hover 变绿
- 数据仍为 Mock，结构改为按年 > 月 > 日 分层组织

### 交互效果
- 点击年份节点 ← 折叠/展开该年
- 点击月份节点 ← 折叠/展开该月
- 点击日记卡片 ← 跳转日记详情
- hover 时有背景色反馈

---

## 2026-05-11 01:22 第6次：写记录页面完善 — 自动获取地点

### 改动内容
[RecordEdit.vue](src/views/RecordEdit.vue) 添加自动定位功能：

- **自动获取地点**：进入写记录页面时，通过浏览器 Geolocation API 自动获取当前位置
- **逆地理编码**：使用 Nominatim (OpenStreetMap) 免费 API 将坐标转为地点名称（城市 · 街道）
- **手动重试**：地点输入框旁有定位按钮，可随时重新获取
- **错误处理**：定位被拒绝 / 不可用 / 超时 均有友好提示，支持手动输入
- 仅新建记录时自动定位，编辑记录时不触发

### 填写规则说明
开发日志后续填写格式统一为：
```
## YYYY-MM-DD HH:MM 第N次：标题

### 地点
涉及的文件或模块

### 改动内容
...
```

每次对代码的操作都必须在日志中记录，注明时间（精确到分钟）、地点（涉及的文件/模块）、当天第几次记录。

---

## 2026-05-11 01:22 第7次：模块重排序 + 共享数据层 + 定位修复

### 改动内容
**1. 侧边栏模块顺序调整**
- 写记录 → 时间线 → 地图
- 默认首页改为写记录页

**2. 共享数据层** [src/stores/records.js](src/stores/records.js)
- 创建了 `useRecords()` 组合式函数，管理所有日记数据
- 支持：增(addRecord)、查(getRecordById)、改(updateRecord)
- 提供 yearTree 计算属性（按年→月→日树形结构），供时间线使用
- 折叠状态独立管理（openYears / openMonths）

**3. 写记录 → 时间线 联动**
- [RecordEdit.vue](src/views/RecordEdit.vue) 保存后写入共享 store
- [Timeline.vue](src/views/Timeline.vue) 从共享 store 读取数据
- [RecordDetail.vue](src/views/RecordDetail.vue) 同样从共享 store 读取，新增编辑按钮
- 提交的记录立即可在时间线中查看

**4. 定位修复** 
- 浏览器 GPS 定位成功 → Nominatim 逆地理编码 → 显示地名
- GPS 失败 → IP 定位兜底 (ip-api.com) → 显示城市
- 添加了 console.log 便于调试定位流程

---

## 2026-05-11 21:49 第8次：定位超时修复 + 地址精度提升

### 改动内容
[RecordEdit.vue](src/views/RecordEdit.vue) 定位逻辑重写：

**1. 修复定位卡死**
- 根本原因：`navigator.geolocation.getCurrentPosition` 在某些浏览器/环境下永不回调，导致 Promise 永远 pending，"定位中..."卡死
- 修复：用 `Promise.race` 加 10 秒超时，GPS 不响应自动切 IP 兜底
- 关闭 `enableHighAccuracy`（室内会导致定位极慢）
- IP-API 改为 HTTPS 请求，避免混合内容拦截

**2. 地址精度提升**
- 地址拼接改为 **小区/社区 → 路 → 区/街道 → 城市** 四级粒度
- Nominatim 数据中包含 `neighbourhood`、`residential`、`suburb`、`road` 等字段
- GPS 成功时可精确到小区级别；GPS 失败时 IP 定位显示城市级别

---

## 2026-05-11 21:53 第9次：逆地理编码切换为高德地图 API

### 改动内容
**数据源切换：OpenStreetMap → 高德地图**
- 之前用的是 OpenStreetMap（Nominatim），在中国数据覆盖差、坐标有偏移
- 现在改为高德地图 Web API，数据精确到小区/楼栋级别

**高德 API 对接：** [src/config.js](src/config.js)
- 新增配置文件，存放高德 API Key
- 地址解析格式：`小区名 · 街道 · 乡镇 · 区 · 城市`
- `coordsys=gps` 参数自动处理 WGS-84 → GCJ-02 坐标转换

**使用前需配置：**
1. 去 https://lbs.amap.com/ 注册 → 控制台 → 创建应用 → 添加 Key（Web 服务）
2. 把 Key 填入 [src/config.js](src/config.js) 的 `AMAP_KEY`

**注意：修复后发现漏了 `coordsys=gps` 参数**（见 第10次记录）

---

## 2026-05-11 21:53 第10次：修复坐标偏移问题

### 改动内容
- **问题**：高德 API 默认接收 GCJ-02（火星坐标系），但 GPS 输出的是 WGS-84
- 两套坐标系在中国有约 500-700 米偏差
- **修复**：API 请求 URL 加上 `&coordsys=gps`，告诉高德传入的是 GPS 坐标，由高德自动转换

---

## 2026-05-11 22:15 第11次：Markdown 编辑器 + 图片插入

### 改动内容
**1. Markdown 编辑器** [RecordEdit.vue](src/views/RecordEdit.vue)
- 编辑区增加了 **编辑 / 预览** 两个 Tab
- 编辑模式：textarea 带 **工具栏**（加粗、斜体、标题、引用、插入图片）
- 预览模式：使用 `marked` 库实时渲染 Markdown → HTML
- 选中文字后点击工具栏按钮，自动包裹 Markdown 语法

**2. 图片插入到正文**
- 点击工具栏「📷 图片」→ 选择图片 → 以 `![描述](base64)` 格式插入到**光标位置**
- 如果光标有选中文字，作为图片描述
- 照片以 base64 嵌入正文，预览和详情页直接渲染

**3. 详情页 Markdown 渲染** [RecordDetail.vue](src/views/RecordDetail.vue)
- 正文同样使用 `marked` 渲染 HTML
- 支持图片、代码块、引用、表格、列表等显示

---

## 2026-05-11 22:24 第12次：博客式编辑器重构 + 图片尺寸控制 + Markdown 导入

### 改动内容
**1. 编辑器改为博客风格** [RecordEdit.vue](src/views/RecordEdit.vue)
- 去掉了「编辑/预览」Tab，保留纯 textarea 编辑
- 界面改为博客写作布局：大标题（32px）、宽正文、干净留白
- 底部显示字数统计
- 工具栏精简，新增删除线、H1/H2/H3、列表、代码块按钮

**2. 图片上传 + 尺寸控制**
- 选择图片后弹出尺寸选择器：小(50%) / 中(75%) / 大(100%)
- 以 `![描述](url =WIDTH)` 格式写入 Markdown
- 详情页 marked 自定义 Renderer 解析尺寸后缀并应用到 `<img>` 标签

**3. Markdown 文件导入**
- 工具栏新增「📂 导入」按钮
- 选择 .md 文件后读取内容，插入到光标位置
- 自动检测本地图片引用并给出控制台提示

---

## 2026-05-11 22:38 第13次：左右分栏实时预览

### 改动内容
[RecordEdit.vue](src/views/RecordEdit.vue) 布局改为**左右分栏**：

**左：编辑 | 右：实时预览**
- 左栏：Monospace 字体的 Markdown 编辑区
- 右栏：`marked` 实时渲染 HTML，图片即时可见
- 自定义 Renderer 支持 `=WIDTH` 图片尺寸后缀
- 两栏各自独立滚动

**图片显示问题**
- base64 嵌入的图片在右侧预览区直接渲染
- 导入 Markdown 中的本地图片引用（相对路径）无法显示，已加检测提示

---

## 2026-05-11 22:48 第14次：图片插入修复 + 自定义日期时间

### 改动内容
**1. 图片插入 Bug 修复**
- 根本原因：`confirmImageSize` 中 `reader.onload` 是异步回调，但 `pendingFile = null` 在同步代码中先执行了
- 回调执行时 `pendingFile` 为 `null`，`pendingFile.name` 报错，插入静默失败
- 修复：提前保存 `file` 和 `desc` 到局部变量，再 `pendingFile = null`

**2. 自定义日期时间**
- 底部栏新增 `<input type="datetime-local">`，默认当前时间
- 支持修改记录日期和时间
- 保存时传入 store 的 `addRecord`，按自定义日期归类到时间线对应位置

---

## 2026-05-12 00:12 第1次：地图模块完善

### 改动内容
**1. Leaflet + OpenStreetMap 地图** [MapView.vue](src/views/MapView.vue)
- 安装 `leaflet` 库，使用 OpenStreetMap 免费在线瓦片
- 有坐标的记录自动在地图上标记绿色圆点
- 点击标记 → 弹窗显示标题、地点、日期
- 点击弹窗 → 跳转记录详情

**2. 侧边栏记录列表**
- 右侧列出所有带位置的记录（实时读取 store）
- 点击侧边栏某项 → 地图飞至该位置并打开弹窗

**3. 记录坐标存储** [stores/records.js](src/stores/records.js)
- 记录新增 `lat` / `lng` 字段
- Mock 数据添加了模拟坐标（北京范围）
- `mapRecords` 计算属性：自动筛选有坐标的记录
- `addRecord` / `updateRecord` 支持传入 `lat` / `lng`
- `getLocation` 现在同时保存坐标，IP 定位也带回坐标



---

## 2026-05-13 第2次：地图模块 — 照片标记 + 聚类 + 当前定位

### 改动内容

**1. Mock 数据改为真实地名和图片** [stores/records.js](src/stores/records.js)
- Mock 数据的地点改为北京真实地名（中关村创业大街、五道口清华科技园、望京SOHO、三里屯太古里、国贸CBD），坐标也随之修正为真实经纬度
- 为有照片的记录添加了 Markdown 正文和示例图片
- 新增 `getFirstImage()` 函数，从 Markdown 正文中提取首张图片 URL
- `mapRecords` 计算属性改为仅返回**有坐标且有照片**的记录，并附带 `firstPhoto` 字段

**2. 地图标记改为照片缩略图** [MapView.vue](src/views/MapView.vue)
- 原有的绿色圆点标记改为**圆形照片缩略图**（56px，白边+阴影，hover 放大效果，类似手机相册地图）
- 点击照片标记直接跳转到记录详情页
- 弹窗中展示照片 + 标题 + 地点 + 日期
- 删除了右侧记录列表，地图改为全宽显示
- 删除了 `flyTo`、`activeId`、`locateFailed` 等侧边栏相关代码

**3. 当前定位** [MapView.vue](src/views/MapView.vue)
- 新增 `addCurrentLocation()` 函数，通过 Geolocation API 获取当前位置
- 用蓝色脉冲动画圆点标记当前定位（类似手机地图"我的位置"）
- 定位失败静默处理，不影响其他功能

**4. 照片聚类（Leaflet.markercluster）** [MapView.vue](src/views/MapView.vue)
- 安装 `leaflet.markercluster` 插件
- 替换 `L.layerGroup` → `L.markerClusterGroup`
- 缩放地图时，同一区域的照片自动合并为一个**照片集群标记**
- 集群标记显示其中一张照片 + 右下角数字角标（表示该区域照片数量）
- 点击集群 → 自动放大展开，逐步显示单张照片标记

**5. 地图视野缓存**
- 离开地图页面时自动保存当前的 center 和 zoom 到模块级变量
- 返回地图时恢复之前的视野，不再每次重置到默认中心
- 用户缩放后点击照片进入详情，返回时地图保持之前缩放后的位置

**6. 修复：返回地图视野重置**
- 问题原因：模块级变量 `savedView` 在组件销毁/重建时可能丢失，且 Leaflet 对象引用存储导致恢复失败
- 修复方案：
  - 将地图视野状态移到 `records.js` 的 reactive `mapViewState` 中，跨组件实例共享
  - 在点击标记跳转前**立即**保存当前 center（存储为纯 `{lat, lng}` 对象）+ zoom
  - `onBeforeUnmount` 作为兜底（只在标记点击未保存时触发）
  - 恢复时使用 `[lat, lng]` 数组语法传给 `setView`

---

## 2026-05-13 第3次：前端 UI 美化

### 改动内容

**1. 设计系统** [style.css](src/style.css)
- 建立 CSS 自定义属性（`:root` 变量）：主色 `#42b983`、卡片白色、阴影、圆角、过渡时长
- 优化全局字体栈（系统原生字体），启用字体抗锯齿
- 美化全局滚动条

**2. 侧边栏大改** [MainLayout.vue](src/views/MainLayout.vue)
- 顶部增加品牌 Logo（绿色圆角方块 + "M"）+ "My Daily" 标题
- 导航链接使用 SVG 图标 + 文字（写记录/时间线/地图）
- 当前路由高亮（绿色背景 + 加粗）
- 用户头像圆形头像圈 + 用户名
- 底部退出按钮（hover 变红）
- 卡片式白底，带分割线

**3. 登录页重做** [Login.vue](src/views/Login.vue)
- 居中卡片布局，渐变色背景
- 品牌 Logo + 标题 + 副标题"记录生活的点滴"
- 表单输入框 focus 时绿色边框 + 光晕
- 登录按钮 hover 凸起 + 阴影动效
- 整页滑入动画

**4. 时间线润色** [Timeline.vue](src/views/Timeline.vue)
- 恢复折叠展开箭头动画（旋转）
- 年份、月份显示计数（N 个月 / N 篇）
- 记录 hover 时卡片浮起 + 阴影
- 圆点 hover 放大 + 变绿
- 标题 hover 变绿
- 整体间距和字号微调

**5. 详情页改卡片式** [RecordDetail.vue](src/views/RecordDetail.vue)
- 文章内容白色卡片 + 阴影 + 圆角
- 标题 26px + 绿色编辑按钮带图标
- 日期、地点用 SVG 图标装饰
- 分隔线分割元信息与正文
- 引用块圆角 + 绿色左边框 + 浅绿背景
- 404 页面居中卡片
- 页面淡入动画

**6. 写记录页 & 地图页同步设计系统**
- 统一使用 CSS 变量替换硬编码颜色值
- 输入框 focus 状态统一绿色光晕
- 按钮 hover 状态统一阴影效果
- 发布、取消按钮样式优化
- 图片尺寸选择弹窗加入毛玻璃背景 + 弹入动画

---

## 2026-05-13 第4次：搭建后端（Phase 1）— FastAPI + SQLite

### 改动内容

**1. 后端项目结构搭建** [backend/](backend/)
- 创建 `backend/` 目录，包含完整的 FastAPI 项目
- 使用 Python venv 管理虚拟环境，`requirements.txt` 声明依赖
- 依赖：fastapi, uvicorn, sqlalchemy, python-jose, passlib, bcrypt

**2. 数据模型** [backend/models.py](backend/models.py)
- **users 表**：id, username（唯一）, hashed_password, nickname, created_at
- **records 表**：id, user_id（外键）, title, content（Markdown 正文）, record_date, location, lat, lng, created_at, updated_at

**3. 用户认证** [backend/auth.py](backend/auth.py) + [routers/auth.py](backend/routers/auth.py)
- JWT 令牌认证（HS256，24 小时过期）
- 密码使用 bcrypt 哈希存储
- 注册 `POST /api/auth/register`、登录 `POST /api/auth/login`
- 接口统一返回 `{ access_token, token_type, user }`

**4. 记录 CRUD** [backend/routers/records.py](backend/routers/records.py)
- `GET /api/records` — 获取当前用户所有记录（按日期倒序）
- `POST /api/records` — 新建记录（标题、正文、地点、坐标、自定义日期）
- `GET /api/records/{id}` — 获取单条记录
- `PUT /api/records/{id}` — 更新记录（支持部分更新）
- `DELETE /api/records/{id}` — 删除记录
- 所有接口均需 Bearer Token 认证，仅返回当前用户数据

**5. 前端 API 对接**
- 新增 [src/api/index.js](src/api/index.js) — fetch 封装，自动附加 JWT token，401 自动跳登录页
- [stores/records.js](src/stores/records.js) 改为异步 API 调用：
  - CRUD 操作全部调用后端接口
  - 新增 `fetchRecords()` 加载所有记录
  - 新增 `deleteRecord()` 删除记录
  - 保留 `yearTree` / `mapRecords` 计算属性（从本地缓存计算）
- [Login.vue](src/views/Login.vue) 改为真实注册/登录，新增 登录/注册 切换 Tab
- [MainLayout.vue](src/views/MainLayout.vue) 挂载时自动调用 `fetchRecords()`
- [RecordDetail.vue](src/views/RecordDetail.vue) 使用 async `getRecordById` 加载数据

### 启动方式

```bash
# 终端 1：后端
cd backend && source venv/bin/activate && uvicorn main:app --reload --port 8000

# 终端 2：前端（另一个终端）
npm run dev
```

后端 API 文档：http://localhost:8000/docs

---

## 2026-05-13 第5次：补充后端依赖记录

### 后端依赖清单

[backend/requirements.txt](backend/requirements.txt) 完整依赖如下：

| 包名 | 版本 | 用途 |
|------|------|------|
| `fastapi` | 0.104.1 | Web 框架，构建 RESTful API |
| `uvicorn` | 0.24.0 | ASGI 服务器，运行 FastAPI |
| `sqlalchemy` | 2.0.23 | ORM，数据库操作 |
| `python-jose[cryptography]` | 3.3.0 | JWT 令牌生成与验证 |
| `passlib[bcrypt]` | 1.7.4 | 密码哈希（bcrypt 算法） |
| `python-multipart` | 0.0.6 | FastAPI 解析 multipart 表单数据（登录等）|

**注意：** `python-multipart` 是 FastAPI 处理表单数据必需的依赖，之前的日志中未记录。

### 安装方式

```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 启动方式

```bash
# 后端（端口 8000）
cd backend && source venv/bin/activate && uvicorn main:app --reload --port 8000

# 前端（端口 5173，另一个终端）
npm run dev
```

---

## 2026-05-14 00:02 第1次：上线联调修复 + 照片 EXIF GPS 定位

### 地点
- [backend/main.py](backend/main.py) — CORS 配置
- [backend/requirements.txt](backend/requirements.txt) — bcrypt 版本锁定
- [src/views/Login.vue](src/views/Login.vue) — API 路径修正
- [src/stores/records.js](src/stores/records.js) — API 路径修正
- [src/views/RecordEdit.vue](src/views/RecordEdit.vue) — EXIF GPS 定位

### 改动内容

**1. 修复 bcrypt 版本不兼容** [backend/requirements.txt](backend/requirements.txt)
- `bcrypt 5.0.0` 与 `passlib 1.7.4` 不兼容，注册/登录接口返回 500
- 降级锁定 `bcrypt==4.0.1`
- 重建 venv 修复目录名变更导致的路径错误（`My Daily` → `My Diary`）

**2. 修复 CORS 跨域问题** [backend/main.py](backend/main.py)
- 前端 dev server 可能运行在 5173 或 5174 端口
- allow_origins 增加 `localhost:5174` 和 `127.0.0.1:5174`

**3. 修复 API 路径前缀缺失** [src/views/Login.vue](src/views/Login.vue) + [src/stores/records.js](src/stores/records.js)
- 前端请求路径为 `/auth/login`，后端实际路由为 `/api/auth/login`
- 所有 API 调用路径加上 `/api` 前缀
- 涉及：登录、注册、记录 CRUD

**4. 照片 EXIF GPS 自动定位** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 安装 `exifr` 库，选择图片时自动读取内嵌 GPS 坐标
- 读到坐标 → 调用高德逆地理编码 → 自动填入地点输入框和坐标字段
- 无 GPS 信息的图片（截图、网图等）静默忽略

**5. 修复地图 leaflet.markercluster 缺失**
- 安装 `leaflet.markercluster` npm 包
- 重启 dev server 清除 Vite 缓存

---

## 2026-05-15 第1次：时间线重写 — 水平日期轴 + 当日内容预览

### 地点
- [src/views/Timeline.vue](src/views/Timeline.vue) — 完全重写

### 改动内容

**1. 布局改为"横轴选日期 + 下面展示内容"** [src/views/Timeline.vue](src/views/Timeline.vue)
- 删除了原有的纵向时间线（年→月→日树形折叠结构）
- 新布局：
  - **顶部**：水平日期轴，按月份分组显示所有有记录的日期
  - **中间**：选中日期的标题（含星期几）+ 记录数量
  - **下方**：该日期所有记录的完整 Markdown 渲染内容

**2. 水平日期轴**
- 白色卡片背景，sticky 固定在顶部
- 按月分组（如"2026年5月"），日期按钮横向排列
- 左右可滚动（隐藏滚动条），选中日期自动滚动到可视区域
- 选中日期高亮为绿色实心圆

**3. 内容区域**
- 每条记录独立卡片，显示完整 Markdown 正文（含图片）
- 点击标题区域 → 跳转记录详情页（箭头图标 hover 有滑动动效）
- 显示地点元信息
- 卡片 hover 阴影加深

**4. 其他改进**
- 右上角新增"写记录"快捷按钮
- 无记录时显示友好的空状态引导
- 自定义 Renderer 支持 `=WIDTH` 图片尺寸后缀（与详情页一致）

---

## 2026-05-16 11:02 第1次：编辑器改造 — 纯文本 + 图片分离

### 地点
- [backend/models.py](backend/models.py) — Record 模型增加 images 字段
- [backend/schemas.py](backend/schemas.py) — 增加 images 字段
- [src/stores/records.js](src/stores/records.js) — 数据模型增加 images
- [src/views/RecordEdit.vue](src/views/RecordEdit.vue) — 完全重写
- [src/views/RecordDetail.vue](src/views/RecordDetail.vue) — 兼容新旧展示

### 改动内容

**1. 编辑器重写** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 删除整个 Markdown 工具栏（加粗、斜体、标题、列表、引用、代码块、导入）
- 删除左右分栏（Markdown 编辑 + 实时预览）
- 改为大的纯文本输入框（无格式，类似微信/小红书风格）
- 下方增加 **图片网格**：3列布局，缩略图显示，hover 可删除
- 支持多张图片同时选择上传
- 图片以 base64 存储在独立的 `images` 字段中，与文字分离

**2. 数据模型分离** [backend/models.py](backend/models.py) + [src/stores/records.js](src/stores/records.js)
- 后端 `records` 表新增 `images` 列（Text，存 JSON 数组）
- 前端 store 同步 `images` 字段，`getFirstImage()` 先查 images 数组再 fallback Markdown
- 为后续对接 FNOS 做好准备

**3. 详情页兼容** [src/views/RecordDetail.vue](src/views/RecordDetail.vue)
- **新记录**（有 images 数组）：纯文本显示 + 下方 3列图片画廊
- **旧记录**（无 images）：继续保持 Markdown 渲染（兼容老数据）
- 点击图片可新窗口打开查看原图

---

## 2026-05-16 11:03 第2次：编辑器图片区优化 + 预览 + 定位按钮调整

### 地点
- [src/views/RecordEdit.vue](src/views/RecordEdit.vue) — 图片网格、预览弹窗、定位按钮

### 改动内容

**1. 图片网格缩小** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 网格改为 4列布局，限制最大宽度 340px
- 减少间距和边距，整体面积缩小约一半

**2. 图片预览** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 点击已添加的图片 → 全屏暗色弹窗显示原图
- 点击遮罩或 ✕ 按钮关闭

**3. 定位按钮优化** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 按钮文字改为 "📍 定位"，避免只显示 emoji 让用户误以为是弹窗
- 定位中时显示 "定位中..."（禁用状态）

---

## 2026-05-16 11:28 第3次：图片网格放大 + 文本区加长 + 地址格式优化

### 地点
- [src/views/RecordEdit.vue](src/views/RecordEdit.vue) — 图片网格、文本区域、地址格式

### 改动内容

**1. 图片网格放大** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 网格改回 3列布局，最大宽度 480px，图片预览更大

**2. 文本输入区加长** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- `min-height` 从 200px 增加到 360px

**3. 地址格式优化** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- `fetchLocationName` 返回格式改为 `城市 · 区 · 街道`（从大到小，更简洁）
- 去除了小区/村镇等细粒度信息

---

## 2026-05-16 11:30 第4次：Bug 修复 — 图片丢失 + 保存不同步 + 删除功能

### 地点
- [backend/routers/records.py](backend/routers/records.py) — create_record 缺少 images 字段
- [backend/routers/auth.py](backend/routers/auth.py) — 新增 verify-password 接口
- [backend/schemas.py](backend/schemas.py) — 新增 VerifyPassword 模型
- [src/api/index.js](src/api/index.js) — 错误对象携带 HTTP 状态码
- [src/views/RecordEdit.vue](src/views/RecordEdit.vue) — saveRecord await + getLocation 不覆盖
- [src/views/RecordDetail.vue](src/views/RecordDetail.vue) — 新增删除按钮 + 密码确认弹窗

### 改动内容

**1. 修复后端创建记录丢失 images 字段** [backend/routers/records.py](backend/routers/records.py)
- **问题**：`create_record` 函数构造 Record 对象时没有传入 `images` 字段
- **后果**：新记录中的图片被静默丢弃，导致 mapRecords 中 `getFirstImage()` 返回 null，记录不在地图中出现
- **修复**：在 Record 构造函数中加上 `images=body.images or "[]"`

**2. 修复保存后记录不出现** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- **问题**：`saveRecord` 是同步函数，调用了 async 的 `addRecord`/`updateRecord` 但不 `await`
- **后果**：`router.push()` 在 API 完成前执行，时间线挂载时 store 中还没有这条记录
- **修复**：改为 `async function saveRecord`，`await` 所有 store 调用，添加 try/catch 错误提示

**3. 修复自动定位覆盖照片 EXIF 坐标** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- **问题**：`getLocation()` 每次调用都会把 `recordLat`/`recordLng` 置为 null
- **后果**：用户先上传带 GPS 照片 → EXIF 设置坐标 → `getLocation(true)` 在 onMounted 中异步完成并清空坐标
- **修复**：`skipIfSet=true` 时不再清空坐标，仅在主动定位时重置

**4. 新增删除功能（密码确认）** [src/views/RecordDetail.vue](src/views/RecordDetail.vue)
- 详情页标题旁新增红色"删除"按钮（带垃圾桶图标）
- 点击弹出密码确认弹窗，输入当前用户的登录密码
- **后端** `POST /api/auth/verify-password`：验证密码是否正确，返回 403 或 ok
- 密码正确 → 调用 `DELETE /api/records/{id}` → 删除成功跳转时间线
- 密码错误 / 网络异常均有错误提示

**5. API 错误对象改进** [src/api/index.js](src/api/index.js)
- 现在 `throw` 的 Error 对象携带 `err.status` 属性（HTTP 状态码）
- 前端可根据状态码区分不同错误类型

---

## 2026-05-16 11:31 第5次：保存按钮 Loading + EXIF 优先定位 + 超时保护

### 地点
- [src/views/RecordEdit.vue](src/views/RecordEdit.vue) — 发布按钮、定位逻辑、超时处理

### 改动内容

**1. 发布按钮 Loading 状态** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 新增 `saving` 状态，点击"发布"后按钮显示"发布中..."并禁用
- 防止重复提交，用户能感知到操作正在进行

**2. 保存超时保护** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 使用 `Promise.race` 加 30 秒超时
- 后端未启动或请求卡住时，提示"保存超时，请检查后端是否启动"
- 超时后恢复按钮可用状态

**3. EXIF GPS 优先** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- **问题**：之前 EXIF GPS 只在 `recordLat` 为 null 时写入，但 GPS/IP 定位先设置了坐标
- **后果**：上传带 GPS 照片后，地点仍显示当前位置而非照片拍摄地
- **修复**：移除 `!recordLat.value` 条件，EXIF GPS 永远覆盖当前坐标
- 用第一张含 GPS 的图片定位，后续图片不再覆盖（`break`）

---

## 2026-05-16 15:40 第6次：大图压缩 + 时间线图片展示

### 地点
- [src/views/RecordEdit.vue](src/views/RecordEdit.vue) — 新增 compressImage 函数
- [src/views/Timeline.vue](src/views/Timeline.vue) — 新增图片网格展示
- [backend/routers/auth.py](backend/routers/auth.py) — 补全 import，修复启动失败

### 改动内容

**1. 大图客户端压缩** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 新增 `compressImage(file)` 函数：超过 1920px 或 2MB 的图片自动缩小
- 使用 Canvas 缩放 + JPEG 0.85 质量，显著减小 base64 体积
- 小图片直接使用原图 base64，避免不必要的质量损失
- 修复 `break` 导致 GPS 之后的图片不添加的 bug（改为 `gpsFound` 标记）

**2. 时间线展示图片** [src/views/Timeline.vue](src/views/Timeline.vue)
- 每条记录正文下方新增图片网格（最多显示 6 张缩略图）
- 超过 6 张显示 "+N" 角标
- 点击图片跳转记录详情页
- 旧 Markdown 记录的图片仍通过 renderedContent 渲染，不受影响

**3. 修复后端导入缺失** [backend/routers/auth.py](backend/routers/auth.py)
- `verify_password_endpoint` 使用了 `get_current_user` 但未导入
- 添加 `get_current_user` 到 import 列表，后端启动恢复正常

---

## 2026-05-16 15:41 第7次：详情页图片点击预览

### 地点
- [src/views/RecordDetail.vue](src/views/RecordDetail.vue) — 图片预览弹窗

### 改动内容

**1. 图片点击放大预览** [src/views/RecordDetail.vue](src/views/RecordDetail.vue)
- 新记录：点击画廊中的图片 → 全屏暗色弹窗显示大图
- 旧记录：点击 Markdown 渲染正文中的图片 → 同样弹出预览
- 点击遮罩或 ✕ 按钮关闭预览
- 复用编辑器中的预览样式，交互一致

---

## 2026-05-16 16:00 第8次：图片预览左右切换 + 地图选点

### 地点
- [src/views/RecordEdit.vue](src/views/RecordEdit.vue) — 预览导航 + 地图选点弹窗
- [src/views/RecordDetail.vue](src/views/RecordDetail.vue) — 预览导航

### 改动内容

**1. 图片预览支持左右切换** [src/views/RecordEdit.vue](src/views/RecordEdit.vue) + [src/views/RecordDetail.vue](src/views/RecordDetail.vue)
- 预览弹窗新增左右箭头按钮（‹ ›），可切换上一张/下一张
- 底部显示 "N / 总数" 计数
- 键盘 ← → 方向键切换，Esc 关闭
- 详情页同时支持新记录画廊和旧 Markdown 内嵌图片的预览

**2. 地图选点功能** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 地点输入框旁新增 "🗺 地图" 按钮
- 点击打开全屏地图弹窗（Leaflet + OpenStreetMap）
- 地图中心默认定位到已有坐标（EXIF GPS → 当前定位 → 北京）
- 点击地图任意位置或拖拽标记 → 高德逆地理编码 → 自动填入地址
- 顶部搜索框（Nominatim）可搜索全球地点
- 点击"确认位置" → 更新地点名称和坐标
- 关闭弹窗清理地图实例，避免内存泄漏

---

## 2026-05-16 16:10 第9次：地图选点优化 — 地址精度 + 搜索下拉 + 我的位置

### 地点
- [src/views/RecordEdit.vue](src/views/RecordEdit.vue) — fetchLocationName + 地图弹窗

### 改动内容

**1. 地址精度提升 + 格式简化** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 格式精简为 `城市 · 区 · 小区/街道`（原来太长）
- **双 API 兜底**：先用高德取 neighborhood，数据不够时自动用 Nominatim 补查
- 确认位置时统一走高德逆地理编码，保持格式一致

**2. 搜索下拉框** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 输入时 400ms 防抖自动搜索（Nominatim）
- 结果以下拉列表展示，最多 6 条
- 点击任意结果 → 地图飞到该位置 + 更新标记
- 保留按 Enter 搜索并自动跳转第一个结果的行为

**3. "我的位置"按钮** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 搜索框旁新增定位按钮（十字准星图标）
- 点击调用浏览器 Geolocation API → 地图飞到当前位置 + 放置标记
- 自动逆地理编码填入地址

---

## 2026-05-16 16:30 第10次：大图处理方案 — 服务端缩略图 + FNOS 原图存储

### 地点
- [backend/main.py](backend/main.py) — 新增 `/api/upload` 图片上传接口 + 静态文件服务
- [backend/requirements.txt](backend/requirements.txt) — 新增 Pillow 依赖
- [src/views/RecordEdit.vue](src/views/RecordEdit.vue) — 改用 multipart 上传，移除客户端压缩
- [src/api/index.js](src/api/index.js) — 导出 API_BASE

### 改动内容

**1. 后端图片上传接口** [backend/main.py](backend/main.py)
- `POST /api/upload`：接收 multipart 文件，保存原图到 `uploads/original/`
- 自动生成 1920px JPEG 缩略图到 `uploads/thumbnail/`（Pillow LANCZOS 缩放 + quality=85）
- 目录通过环境变量 `UPLOAD_DIR` 配置，默认为 `./uploads`
- 后续对接 FNOS：只需将 `UPLOAD_DIR` 指向 NAS 挂载目录即可
- 挂载静态文件服务 `/uploads`，可直接通过 URL 访问图片

**2. 前端改用 multipart 上传** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 移除客户端 `compressImage()` 函数（改为服务端 Pillow 处理）
- `onImagesSelected` 改为 FormData 逐张上传到 `/api/upload`
- 存储缩略图 URL 到 images 数组（如 `http://localhost:8000/uploads/thumbnail/xxx.jpg`）
- 新增 `getOriginalUrl(img)`：缩略图 URL 替换为原图 URL，用于预览大图
- 导入 `API_BASE` 用于拼接完整 URL

**3. 依赖更新** [backend/requirements.txt](backend/requirements.txt)
- 新增 `Pillow==10.2.0` — Python 图片处理库（缩略图生成）

### FNOS 对接说明
后续将 `UPLOAD_DIR` 环境变量指向 FNOS 挂载目录：
```bash
export UPLOAD_DIR="/mnt/fnos/MyDiary/uploads"
```
- 原图自动保存到 FNOS，可在 FNOS 相册中查看
- 缩略图同样存到 FNOS，应用内展示用缩略图，流量小、加载快
- 查看原图时通过 `/uploads/original/{name}` 获取

---

## 2026-05-20 22:52 第1次：地图瓦片切换 — OpenStreetMap → 高德地图

### 发现的问题
地图页（MapView）和编辑器地图选点弹窗使用 OpenStreetMap 瓦片（`tile.openstreetmap.org`），服务器在境外。**在中国大陆直连时，瓦片加载极慢甚至完全加载不出（显示灰色网格）**，开启 VPN/代理后正常。影响两个页面：地图记录页和写记录的地图选点弹窗。

### 解决方案
将地图瓦片源从 OpenStreetMap 切换为**高德地图瓦片**，同时引入坐标转换：

| 方面 | 之前 | 之后 |
|------|------|------|
| 瓦片源 | `tile.openstreetmap.org` | `webrd0{1-4}.is.autonavi.com`（高德） |
| 坐标系 | WGS-84（存储 + 显示一致） | 存储 WGS-84，显示转 GCJ-02 |
| 境内加载 | ❌ 极慢/不加载 | ✅ 快速加载 |

### 改动内容

**1. 坐标转换工具** [src/utils/coord.js](src/utils/coord.js)
- 新建文件，实现 WGS-84 ↔ GCJ-02 互转算法
- `wgs84ToGcj02()`：将 GPS 原始坐标转为高德火星坐标系
- `gcj02ToWgs84()`：将火星坐标系转回 GPS 坐标（迭代法，精度 ~0.5 米）
- 中国境外坐标自动跳过转换，不影响国外使用

**2. 地图页** [src/views/MapView.vue](src/views/MapView.vue)
- 瓦片 URL 改为高德地图：`https://webrd0{s}.is.autonavi.com/appmaptile?lang=zh_cn&size=1&scale=1&style=8`
- 记录标记坐标：从 DB 读取 WGS-84 → `wgs84ToGcj02()` 转换后显示
- 当前定位蓝点：浏览器 GPS（WGS-84）→ 转换后显示

**3. 编辑器地图选点** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 瓦片 URL 同样改为高德地图
- **搜索**（Nominatim 返回 WGS-84）→ 转 GCJ-02 显示在地图上
- **拖拽/点击**（从高德瓦片获取 GCJ-02）→ 转 WGS-84 后查地址和存储
- **我的位置**（浏览器 GPS 返回 WGS-84）→ 转 GCJ-02 显示
- **确认位置** → GCJ-02 转回 WGS-84 存入 `recordLat`/`recordLng`

### 验证方法
1. 关闭代理 → 打开地图页 → 瓦片应快速加载，无灰色格子
2. 地图上的照片标记位置与之前 OSM 时基本一致（几百米级偏差由坐标系差异引起）
3. 编辑器地图选点：搜索位置、拖拽标记、我的位置均能正确逆地理编码
4. 保存记录后，时间线和详情页显示的位置信息不变（存储层仍是 WGS-84）

---

## 2026-05-20 23:10 第2次：修复地址不显示小区名称

### 发现的问题
定位地址格式为 `城市 · 区`，缺少小区名。即使地图瓦片上能看到小区名称，逆地理编码返回的地址也没有小区信息。

### 根本原因
高德 `regeo` API 返回的 `addressComponent.neighborhood.name` 在很多位置是**空数组 `[]`** 而非空字符串。JavaScript 中 `[]` 是 truthy 值，导致 `ac.neighborhood?.name || fallback` 永远取到 `[]`，不会进入兜底逻辑。

### 改动内容

**修复** [src/views/RecordEdit.vue](src/views/RecordEdit.vue) — `amapReverse` 函数重写

| 级别 | 方式 | 说明 |
|------|------|------|
| 1 | `neighborhood.name` | 修复空数组判断：`Array.isArray(nh) && nh.length === 0` |
| 2 | **附近 POI 搜索** | 改用 `extensions=all`，从返回的 POI 列表中找 `住宅小区`/`住宅区` 类型 |
| 3 | `township` | 没有小区时用街道办/乡镇名保底 |

关键修复点：`neighborhood?.name` 的类型可能是数组而非字符串，需做数组判空处理。

### 验证
- 宜春市政府附近 → `宜春市 · 袁州区 · 崔家园` ✅
- 宜春翡翠城附近 → `宜春市 · 袁州区 · 翡翠城` ✅

---

## 2026-05-20 23:28 第3次：时间线重构 — 纵向时间轴

### 改动内容
[src/views/Timeline.vue](src/views/Timeline.vue) — 完全重写

**1. 布局改造：横轴选日期 → 纵向时间轴**
- 删除原有的水平日期轴（按月分组、横向滚动选日期）
- 改为纵向时间轴，左侧一条绿色竖线贯穿
- 按 **年 → 月 → 日** 三级树形结构展示
- 年份可折叠/展开月份，月份可折叠/展开日期
- 只有有记录的日期才显示在时间轴上（无记录日期不显示）
- 倒序排列（最新日期在最上面）

**2. 时间轴动画效果**
- **线条绘制动画**：页面加载时绿色竖线从顶部到底部逐渐绘制（`height: 0 → 100%` transition）
- **节点入场动画**：每个节点依次淡入并向右滑入（`nodeFadeIn`，延迟递增）
- **选中脉冲动画**：选中日期的圆点有呼吸光晕效果（`dotPulse`）
- **内容展开动画**：选中日期后内容卡片淡入下滑（`contentSlide`）

**3. 记录展示**
- 点击日期 → 右侧展开该日记录卡片
- 卡片保留原有功能：标题可跳转详情、地点、Markdown 正文渲染、图片网格

---

## 2026-05-20 23:42 第4次：时间轴改为单根横轴 + macOS Dock 悬停放大效果

### 改动内容
[src/views/Timeline.vue](src/views/Timeline.vue) — 完全重写

**1. 改为单根横向时间轴**
- 删除多行月份横排，改为**单根时间轴**，年份、月份、日期全部放在同一根线上
- 扁平化节点列表：`年份标记 → 月份标记 → 日期圆点 → 月份标记 → 日期圆点 → ...`
- 年份标记左侧有竖线分隔 + 粗体大字 + 篇数
- 月份标记有左边框分隔 + 灰色文字，穿插在日期之间
- 整根轴一条渐变横线贯穿（pointer-events: none）
- 横向滚动可左右拖动（cursor: grab）
- 无记录的时间段不占用空间

**2. 按日期比例计算间距**
- 日期圆点的水平间距**按实际时间差比例计算**（14px/天）
- 相邻日期间隔至少 48px 防止重叠
- 年份和月份标记自动定位在所属第一个日期之前
- 滚动宽度自适应，总宽度 = 最后日期位置 + 边距

**2. macOS Dock 悬停放大效果**
- 鼠标在时间轴上移动时，通过 `requestAnimationFrame` 实时计算每个圆点到鼠标的距离
- 余弦插值算法：离鼠标越近放大越大（最近 1.65 倍），相邻圆点过渡缩小
- 鼠标离开时所有圆点恢复原始大小
- `will-change: transform` 优化渲染性能

**3. 展示优化**
- 日期圆点默认仅数字，hover 时底部弹出 `日 · 篇数`
- 选中日期圆点变绿色 + 光晕
- 下方内容卡片跟随切换（淡入动画）

---

## 2026-05-21 21:03 第1次：时间轴导航 + 自适应窗口 + 等间距重构

### 地点
- [src/views/Timeline.vue](src/views/Timeline.vue) — 翻页箭头、Bug 修复、拖拽、窗口、等间距
- [src/views/MapView.vue](src/views/MapView.vue) — 排查原因

### 改动内容

**1. 修复时间线不显示 Bug**
- **问题**：模板中 `v-if="tl.nodes.length > 0"` 使用了不存在的 `.nodes` 属性
- **根本原因**：`tl` 计算属性返回 `{ years, months, dates, totalWidth }`，没有 `nodes` 字段
- **修复**：改为 `tl.dates.length > 0`

**2. 鼠标拖拽与滑动时间窗口**
- 移除 ◀ ▶ 翻页按钮，隐藏滚动条，改为鼠标拖拽滑动
- 最终方案：**等间距滑动窗口**，一次最多显示 10 个时间点
- 拖拽位移换算为下标位移（1 个时间点宽度 ≈ 容器宽度 / 10）

**3. 默认定位到最新记录**
- 页面加载后时间轴默认显示最新的记录（窗口末尾）

**4. 地图模块排查**
- 数据库中 4 条测试记录的 `images` 均为 `"[]"`，`getFirstImage()` 返回 null
- **结论**：地图只展示带照片的记录，不带图不会显示

---

## 2026-05-21 21:35 第2次：时间轴等间距 + 测试数据 + 全国分布 + 拖拽修复

### 地点
- [src/views/Timeline.vue](src/views/Timeline.vue) — 等间距排列算法
- 后端 API — 批量创建测试记录

### 改动内容

**1. 时间轴重构为等间距排列**
- 之前的算法（按时间比例定位 + 自适应窗口）性能太卡，改为**固定每次最多显示 10 个时间点，等间距排列**
- 位置 = `index * (容器宽度 / (显示数量 - 1))`，即均匀分布
- 渲染节点数固定最多 10 个，大幅提升性能
- 拖拽时按"下标位移"滑动窗口（1 个位置宽度 ≈ 容器宽度 / 10）

**2. 批量创建 20 条测试记录**
- 日期横跨 2025-01 到 2026-05，覆盖 2 个年份
- 每年各月分布均匀（2025 年 12 个月 + 2026 年 5 个月 + 密集测试）
- 2026 年 5 月有多条记录（5/1、5/5、5/10、5/16）用于测试密集场景

**3. 测试数据分布到全国各地 + 风景图片**
- 20 条记录分布到哈尔滨、北京、西安、杭州、成都、桂林、丽江、拉萨、武汉、广州、南京、重庆、三亚、青岛、厦门、长沙、洛阳、兰州、昆明、上海
- 每个记录包含：真实地点名（如"北京市 · 故宫博物院"）+ 坐标（当地经纬度）
- 每个记录附带一张 SVG 渐变风景风格图片（城市名 + 渐变色背景），存储在 `images` 字段
- 地图页现在应该可以显示全国各地的照片标记了

**4. 修复时间轴拖拽不工作**
- 根因：`mousemove`/`mouseup` 绑定在元素上，鼠标一离开元素就收不到事件
- 修复：`mousedown` 时在 `window` 上注册 `mousemove`/`mouseup` 监听器，拖拽结束自动移除
- `mouseleave` 不再停止拖拽，只还原 Dock 效果

**5. 清除 `tl` 计算属性中的副作用**
- `windowStartIdx` 的初始化从 `tl` 计算属性移到 `onMounted`，避免计算属性中修改 ref 的 Vue 反模式

---

## 2026-05-23 00:10 第1次：修复时间轴和地图的异步数据加载竞争条件

### 背景
上一轮改动后，用户反馈时间轴无法滑动、地图不显示新增的20个记录点。排查发现这两个问题的根因相同：**数据加载竞争条件**。

### 根因
- `MainLayout.vue` 的 `onMounted` 中异步调用 `fetchRecords()`（从后端加载所有记录）
- `Timeline.vue` 和 `MapView.vue` 的 `onMounted` 执行时，`fetchRecords` 尚未完成
- 此时 `recordsMap` 为空，导致 `sortedDates` / `mapRecords` 均为空
- 两个组件都在 `onMounted` 中做初始化，看不到数据就跳过了初始化逻辑
- 数据到达后、Vue 响应式更新了计算属性，但没有代码重新触发初始化

### 改动

**1. [src/views/Timeline.vue](src/views/Timeline.vue) — 用 `watch` 替代 `onMounted` 初始化**
- 在 `onMounted` 中通过 `sortedDates.value.length` 判断数据是否就绪 → 数据异步加载时必为空
- 改为 `watch(sortedDates, callback)`：Vue 响应式监听 `sortedDates` 变化
- 数据到达后自动执行回调：设置 `windowStartIdx` 定位到最新记录、`selectedDate` 选中最新日期
- `windowStartIdx` 保护条件（值为 0 且记录数 > DISPLAY_COUNT）确保用户拖拽后不会重置

**2. [src/views/MapView.vue](src/views/MapView.vue) — 用 `watch` + `immediate: true` 初始化地图**
- `initMap()` 在 `onMounted` 中被调用时 `mapRecords` 为空，直接 `return`
- 改为 `watch(() => mapRecords.value.length, callback, { immediate: true })`
- `immediate: true` 覆盖数据已就绪的情况（用户从其他路由切换过来时）
- 数据异步到达后自动调用 `initMap()` + `addCurrentLocation()`
