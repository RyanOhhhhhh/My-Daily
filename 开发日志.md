2026-05-11 开发前的要求
我是开发小白，我现在想开发一个日常生活记录软件，这个软件可以先做网页版，后续再做安卓版本。 先说下我想开发的一些需求：
一、基础模块： 
1.有登录界面，并且对登录进行一个验证。 
2.生活记录界面，最好是像markdown一样，可以记录日常的文字，同时可以上传照片，照片最好是能和文字在一起呈现，并且要有时间和地点记录 
3.时间线界面，之前的日常记录通过时间线可以进行一个排序，有年月日的时间线，并且可以记录哪天记录了，哪天没记录，最好使用记录的标题来呈现，点击标题会跳转到详细的记录。 
4.地图记录界面，之前的日常记录，通过地图的方式呈现，靠之前的记录的地点在地图上做好标记，并且通过上传的照片在地图上呈现，免费最好是在线地图。
二、功能要求：
1.界面要尽量简约美观
2.前后端分离，前端负责记录和计算，后端负责存储数据
3.存储方面的话，我想着云端存储比较方便，在哪都可以，但是最好是先拿本地存储试一下，之后想办法在云服务器和本地nas之间都进行数据存储
4.以后生成了什么，做了什么，都在开发日志.md里面做好记录以及时间，方便后续迭代。
先别进行开发，在开发前先询问下我的意见

---

## 2026-05-11 开发框架总纲

### 一、整体架构

```
┌─────────────────────────────────────────────────┐
│                   前端 (Web)                       │
│  React / Vue + 路由 + 状态管理 + HTTP 请求        │
└────────────────────┬────────────────────────────┘
                     │ API 通信
┌────────────────────▼────────────────────────────┐
│                后端 (API Server)                   │
│  Node.js / Python + RESTful API                  │
└────────────────────┬────────────────────────────┘
                     │ 数据读写
┌────────────────────▼────────────────────────────┐
│                  数据层                           │
│  本地: SQLite / JSON 文件                        │
│  云端: MySQL / PostgreSQL                        │
│  NAS : 后续扩展                                   │
└─────────────────────────────────────────────────┘
```

### 二、推荐技术栈

| 层级 | 推荐方案 | 说明 |
|------|----------|------|
| 前端框架 | ✅ Vue 3 | |
| UI 组件库 | Element Plus / Naive UI | 简约美观 |
| 地图 | Leaflet + OpenStreetMap | 免费在线地图，轻量 |
| 富文本编辑 | 自建 Markdown 编辑器 | 类 Markdown 写作体验 |
| 后端框架 | ✅ Python (FastAPI) | 轻量快速，自动生成 API 文档 |
| 本地数据库 | ✅ SQLite | 无需服务，文件即库 |
| 云端数据库 | MySQL / PostgreSQL | 后续迁移 |
| 图片存储 | 本地文件 → 后续 OSS / NAS | |

### 三、模块划分

#### 1. 用户模块
- 注册 / 登录 / 登出
- 登录态验证（JWT Token）
- 用户信息管理

#### 2. 日记记录模块
- 创建 / 编辑 / 删除日记
- Markdown 编辑器（标题 + 正文）
- 图片上传，文中穿插展示
- 自动记录时间 + 地点（IP 定位 / 手动选择）
- 日记详情页

#### 3. 时间线模块
- 按年 → 月 → 日 三级导航
- 日历视图，标记有记录的日子
- 显示记录标题列表
- 点击标题跳转详情

#### 4. 地图模块
- Leaflet + OpenStreetMap 在线地图
- 有地点的记录在地图打标
- 点击标记显示标题 + 照片缩略图
- 点击跳转详情

### 四、前后端 API 概要

```
POST   /api/auth/register     注册
POST   /api/auth/login        登录
GET    /api/records           获取日记列表（支持时间筛选）
POST   /api/records           创建日记
GET    /api/records/:id       获取单条日记
PUT    /api/records/:id       编辑日记
DELETE /api/records/:id       删除日记
POST   /api/upload            上传图片
GET    /api/records/timeline  获取时间线数据
GET    /api/records/map       获取地图标记数据
```

### 五、开发路线图

| 阶段 | 内容 | 说明 |
|------|------|------|
| Phase 1 | 基础框架搭建 | 前后端脚手架、建表、登录注册 |
| Phase 2 | 日记记录核心功能 | Markdown 编辑器、图片上传、时间地点 |
| Phase 3 | 时间线视图 | 日历导航、日期标记、标题列表 |
| Phase 4 | 地图视图 | 地图集成、定位标记 |
| Phase 5 | UI 美化与细节打磨 | |
| Phase 6 | 本地转云端存储 | 数据库迁移、服务器部署 |
| Phase 7 | Android 版本 | WebView 或原生重写 |

### 六、数据表设计（概要）

**users 表**：id, 用户名, 密码(加密), 昵称, 创建时间

**records 表**：id, 用户id, 标题, 正文(markdown), 记录时间, 地点名称, 经度, 纬度, 创建时间, 更新时间

**photos 表**：id, 记录id, 图片路径/URL, 上传时间

---

*以上为框架总纲，后续各阶段细化具体实现。*

---

## 2026-05-11 Phase 1：前端脚手架搭建完成

已完成：
- 使用 Vite 创建 Vue 3 项目（`frontend/`）
- 安装 vue-router@4
- 开发服务器 `http://localhost:5173/` 运行正常

下一步：
- 安装 UI 组件库（Element Plus / Naive UI 待选）
- 搭建前端路由框架（登录页、首页等）
- 搭建后端 Python FastAPI 项目

---

## 2026-05-11 Phase 1：前端核心页面框架搭建完成

已完成：
- 创建 `src/router/index.js` — 路由配置，含登录守卫
- 创建 `src/views/Login.vue` — 登录页
- 创建 `src/views/MainLayout.vue` — 主布局（侧边栏导航 + 内容区）
- 创建 `src/views/Timeline.vue` — 时间线页面（年导航 + 月网格 + 记录列表）
- 创建 `src/views/MapView.vue` — 地图页面（占位，后续集成 Leaflet）
- 创建 `src/views/RecordEdit.vue` — 记录编辑页（标题 + 地点 + Markdown 正文 + 照片）
- 创建 `src/views/RecordDetail.vue` — 记录详情页

当前页面路由：
| 路由 | 页面 |
|------|------|
| `/login` | 登录页 |
| `/timeline` | 时间线（首页默认） |
| `/map` | 地图视图 |
| `/record/new` | 新建记录 |
| `/record/:id` | 记录详情 |
| `/record/:id/edit` | 编辑记录 |

数据目前为本地 Mock，后续对接后端 API。


## 2026-05-11 Phase 1：git了一下
创建了git项目My-DIARY

---

## 2026-05-11 Phase 1：时间线模块重构 — 纵向时间线 + 折叠效果

### 改动内容
重写 [Timeline.vue](src/views/Timeline.vue)，将原来的年/月/月历卡片式布局改为**纵向时间线**布局：

- **纵向时间线**：左侧一条竖线贯穿，年份、月份、日记条目沿线排列
- **折叠展开**：
  - 年份节点可折叠/展开其月份列表
  - 月份节点可折叠/展开其日记列表
  - 折叠状态通过圆点填充状态和箭头方向直观展示
- **视觉层次**：
  - 年份：大实心圆（28px）+ 加粗大字
  - 月份：中小实心圆（16px）+ 中等字号
  - 日记：小圆点（10px）+ 卡片样式，hover 变绿
- 数据仍为 Mock，结构改为按年 > 月 > 日 分层组织

### 交互效果
- 点击年份节点 ← 折叠/展开该年
- 点击月份节点 ← 折叠/展开该月
- 点击日记卡片 ← 跳转日记详情
- hover 时有背景色反馈

---

## 2026-05-11 01:22 第6次：写记录页面完善 — 自动获取地点

### 改动内容
[RecordEdit.vue](src/views/RecordEdit.vue) 添加自动定位功能：

- **自动获取地点**：进入写记录页面时，通过浏览器 Geolocation API 自动获取当前位置
- **逆地理编码**：使用 Nominatim (OpenStreetMap) 免费 API 将坐标转为地点名称（城市 · 街道）
- **手动重试**：地点输入框旁有定位按钮，可随时重新获取
- **错误处理**：定位被拒绝 / 不可用 / 超时 均有友好提示，支持手动输入
- 仅新建记录时自动定位，编辑记录时不触发

### 填写规则说明
开发日志后续填写格式统一为：
```
## YYYY-MM-DD HH:MM 第N次：标题

### 地点
涉及的文件或模块

### 改动内容
...
```

每次对代码的操作都必须在日志中记录，注明时间（精确到分钟）、地点（涉及的文件/模块）、当天第几次记录。

---

## 2026-05-11 01:22 第7次：模块重排序 + 共享数据层 + 定位修复

### 改动内容
**1. 侧边栏模块顺序调整**
- 写记录 → 时间线 → 地图
- 默认首页改为写记录页

**2. 共享数据层** [src/stores/records.js](src/stores/records.js)
- 创建了 `useRecords()` 组合式函数，管理所有日记数据
- 支持：增(addRecord)、查(getRecordById)、改(updateRecord)
- 提供 yearTree 计算属性（按年→月→日树形结构），供时间线使用
- 折叠状态独立管理（openYears / openMonths）

**3. 写记录 → 时间线 联动**
- [RecordEdit.vue](src/views/RecordEdit.vue) 保存后写入共享 store
- [Timeline.vue](src/views/Timeline.vue) 从共享 store 读取数据
- [RecordDetail.vue](src/views/RecordDetail.vue) 同样从共享 store 读取，新增编辑按钮
- 提交的记录立即可在时间线中查看

**4. 定位修复** 
- 浏览器 GPS 定位成功 → Nominatim 逆地理编码 → 显示地名
- GPS 失败 → IP 定位兜底 (ip-api.com) → 显示城市
- 添加了 console.log 便于调试定位流程

---

## 2026-05-11 21:49 第8次：定位超时修复 + 地址精度提升

### 改动内容
[RecordEdit.vue](src/views/RecordEdit.vue) 定位逻辑重写：

**1. 修复定位卡死**
- 根本原因：`navigator.geolocation.getCurrentPosition` 在某些浏览器/环境下永不回调，导致 Promise 永远 pending，"定位中..."卡死
- 修复：用 `Promise.race` 加 10 秒超时，GPS 不响应自动切 IP 兜底
- 关闭 `enableHighAccuracy`（室内会导致定位极慢）
- IP-API 改为 HTTPS 请求，避免混合内容拦截

**2. 地址精度提升**
- 地址拼接改为 **小区/社区 → 路 → 区/街道 → 城市** 四级粒度
- Nominatim 数据中包含 `neighbourhood`、`residential`、`suburb`、`road` 等字段
- GPS 成功时可精确到小区级别；GPS 失败时 IP 定位显示城市级别

---

## 2026-05-11 21:53 第9次：逆地理编码切换为高德地图 API

### 改动内容
**数据源切换：OpenStreetMap → 高德地图**
- 之前用的是 OpenStreetMap（Nominatim），在中国数据覆盖差、坐标有偏移
- 现在改为高德地图 Web API，数据精确到小区/楼栋级别

**高德 API 对接：** [src/config.js](src/config.js)
- 新增配置文件，存放高德 API Key
- 地址解析格式：`小区名 · 街道 · 乡镇 · 区 · 城市`
- `coordsys=gps` 参数自动处理 WGS-84 → GCJ-02 坐标转换

**使用前需配置：**
1. 去 https://lbs.amap.com/ 注册 → 控制台 → 创建应用 → 添加 Key（Web 服务）
2. 把 Key 填入 [src/config.js](src/config.js) 的 `AMAP_KEY`

**注意：修复后发现漏了 `coordsys=gps` 参数**（见 第10次记录）

---

## 2026-05-11 21:53 第10次：修复坐标偏移问题

### 改动内容
- **问题**：高德 API 默认接收 GCJ-02（火星坐标系），但 GPS 输出的是 WGS-84
- 两套坐标系在中国有约 500-700 米偏差
- **修复**：API 请求 URL 加上 `&coordsys=gps`，告诉高德传入的是 GPS 坐标，由高德自动转换

---

## 2026-05-11 22:15 第11次：Markdown 编辑器 + 图片插入

### 改动内容
**1. Markdown 编辑器** [RecordEdit.vue](src/views/RecordEdit.vue)
- 编辑区增加了 **编辑 / 预览** 两个 Tab
- 编辑模式：textarea 带 **工具栏**（加粗、斜体、标题、引用、插入图片）
- 预览模式：使用 `marked` 库实时渲染 Markdown → HTML
- 选中文字后点击工具栏按钮，自动包裹 Markdown 语法

**2. 图片插入到正文**
- 点击工具栏「📷 图片」→ 选择图片 → 以 `![描述](base64)` 格式插入到**光标位置**
- 如果光标有选中文字，作为图片描述
- 照片以 base64 嵌入正文，预览和详情页直接渲染

**3. 详情页 Markdown 渲染** [RecordDetail.vue](src/views/RecordDetail.vue)
- 正文同样使用 `marked` 渲染 HTML
- 支持图片、代码块、引用、表格、列表等显示

---

## 2026-05-11 22:24 第12次：博客式编辑器重构 + 图片尺寸控制 + Markdown 导入

### 改动内容
**1. 编辑器改为博客风格** [RecordEdit.vue](src/views/RecordEdit.vue)
- 去掉了「编辑/预览」Tab，保留纯 textarea 编辑
- 界面改为博客写作布局：大标题（32px）、宽正文、干净留白
- 底部显示字数统计
- 工具栏精简，新增删除线、H1/H2/H3、列表、代码块按钮

**2. 图片上传 + 尺寸控制**
- 选择图片后弹出尺寸选择器：小(50%) / 中(75%) / 大(100%)
- 以 `![描述](url =WIDTH)` 格式写入 Markdown
- 详情页 marked 自定义 Renderer 解析尺寸后缀并应用到 `<img>` 标签

**3. Markdown 文件导入**
- 工具栏新增「📂 导入」按钮
- 选择 .md 文件后读取内容，插入到光标位置
- 自动检测本地图片引用并给出控制台提示

---

## 2026-05-11 22:38 第13次：左右分栏实时预览

### 改动内容
[RecordEdit.vue](src/views/RecordEdit.vue) 布局改为**左右分栏**：

**左：编辑 | 右：实时预览**
- 左栏：Monospace 字体的 Markdown 编辑区
- 右栏：`marked` 实时渲染 HTML，图片即时可见
- 自定义 Renderer 支持 `=WIDTH` 图片尺寸后缀
- 两栏各自独立滚动

**图片显示问题**
- base64 嵌入的图片在右侧预览区直接渲染
- 导入 Markdown 中的本地图片引用（相对路径）无法显示，已加检测提示

---

## 2026-05-11 22:48 第14次：图片插入修复 + 自定义日期时间

### 改动内容
**1. 图片插入 Bug 修复**
- 根本原因：`confirmImageSize` 中 `reader.onload` 是异步回调，但 `pendingFile = null` 在同步代码中先执行了
- 回调执行时 `pendingFile` 为 `null`，`pendingFile.name` 报错，插入静默失败
- 修复：提前保存 `file` 和 `desc` 到局部变量，再 `pendingFile = null`

**2. 自定义日期时间**
- 底部栏新增 `<input type="datetime-local">`，默认当前时间
- 支持修改记录日期和时间
- 保存时传入 store 的 `addRecord`，按自定义日期归类到时间线对应位置

---

## 2026-05-12 00:12 第1次：地图模块完善

### 改动内容
**1. Leaflet + OpenStreetMap 地图** [MapView.vue](src/views/MapView.vue)
- 安装 `leaflet` 库，使用 OpenStreetMap 免费在线瓦片
- 有坐标的记录自动在地图上标记绿色圆点
- 点击标记 → 弹窗显示标题、地点、日期
- 点击弹窗 → 跳转记录详情

**2. 侧边栏记录列表**
- 右侧列出所有带位置的记录（实时读取 store）
- 点击侧边栏某项 → 地图飞至该位置并打开弹窗

**3. 记录坐标存储** [stores/records.js](src/stores/records.js)
- 记录新增 `lat` / `lng` 字段
- Mock 数据添加了模拟坐标（北京范围）
- `mapRecords` 计算属性：自动筛选有坐标的记录
- `addRecord` / `updateRecord` 支持传入 `lat` / `lng`
- `getLocation` 现在同时保存坐标，IP 定位也带回坐标



---

## 2026-05-13 第2次：地图模块 — 照片标记 + 聚类 + 当前定位

### 改动内容

**1. Mock 数据改为真实地名和图片** [stores/records.js](src/stores/records.js)
- Mock 数据的地点改为北京真实地名（中关村创业大街、五道口清华科技园、望京SOHO、三里屯太古里、国贸CBD），坐标也随之修正为真实经纬度
- 为有照片的记录添加了 Markdown 正文和示例图片
- 新增 `getFirstImage()` 函数，从 Markdown 正文中提取首张图片 URL
- `mapRecords` 计算属性改为仅返回**有坐标且有照片**的记录，并附带 `firstPhoto` 字段

**2. 地图标记改为照片缩略图** [MapView.vue](src/views/MapView.vue)
- 原有的绿色圆点标记改为**圆形照片缩略图**（56px，白边+阴影，hover 放大效果，类似手机相册地图）
- 点击照片标记直接跳转到记录详情页
- 弹窗中展示照片 + 标题 + 地点 + 日期
- 删除了右侧记录列表，地图改为全宽显示
- 删除了 `flyTo`、`activeId`、`locateFailed` 等侧边栏相关代码

**3. 当前定位** [MapView.vue](src/views/MapView.vue)
- 新增 `addCurrentLocation()` 函数，通过 Geolocation API 获取当前位置
- 用蓝色脉冲动画圆点标记当前定位（类似手机地图"我的位置"）
- 定位失败静默处理，不影响其他功能

**4. 照片聚类（Leaflet.markercluster）** [MapView.vue](src/views/MapView.vue)
- 安装 `leaflet.markercluster` 插件
- 替换 `L.layerGroup` → `L.markerClusterGroup`
- 缩放地图时，同一区域的照片自动合并为一个**照片集群标记**
- 集群标记显示其中一张照片 + 右下角数字角标（表示该区域照片数量）
- 点击集群 → 自动放大展开，逐步显示单张照片标记

**5. 地图视野缓存**
- 离开地图页面时自动保存当前的 center 和 zoom 到模块级变量
- 返回地图时恢复之前的视野，不再每次重置到默认中心
- 用户缩放后点击照片进入详情，返回时地图保持之前缩放后的位置

**6. 修复：返回地图视野重置**
- 问题原因：模块级变量 `savedView` 在组件销毁/重建时可能丢失，且 Leaflet 对象引用存储导致恢复失败
- 修复方案：
  - 将地图视野状态移到 `records.js` 的 reactive `mapViewState` 中，跨组件实例共享
  - 在点击标记跳转前**立即**保存当前 center（存储为纯 `{lat, lng}` 对象）+ zoom
  - `onBeforeUnmount` 作为兜底（只在标记点击未保存时触发）
  - 恢复时使用 `[lat, lng]` 数组语法传给 `setView`

---

## 2026-05-13 第3次：前端 UI 美化

### 改动内容

**1. 设计系统** [style.css](src/style.css)
- 建立 CSS 自定义属性（`:root` 变量）：主色 `#42b983`、卡片白色、阴影、圆角、过渡时长
- 优化全局字体栈（系统原生字体），启用字体抗锯齿
- 美化全局滚动条

**2. 侧边栏大改** [MainLayout.vue](src/views/MainLayout.vue)
- 顶部增加品牌 Logo（绿色圆角方块 + "M"）+ "My Daily" 标题
- 导航链接使用 SVG 图标 + 文字（写记录/时间线/地图）
- 当前路由高亮（绿色背景 + 加粗）
- 用户头像圆形头像圈 + 用户名
- 底部退出按钮（hover 变红）
- 卡片式白底，带分割线

**3. 登录页重做** [Login.vue](src/views/Login.vue)
- 居中卡片布局，渐变色背景
- 品牌 Logo + 标题 + 副标题"记录生活的点滴"
- 表单输入框 focus 时绿色边框 + 光晕
- 登录按钮 hover 凸起 + 阴影动效
- 整页滑入动画

**4. 时间线润色** [Timeline.vue](src/views/Timeline.vue)
- 恢复折叠展开箭头动画（旋转）
- 年份、月份显示计数（N 个月 / N 篇）
- 记录 hover 时卡片浮起 + 阴影
- 圆点 hover 放大 + 变绿
- 标题 hover 变绿
- 整体间距和字号微调

**5. 详情页改卡片式** [RecordDetail.vue](src/views/RecordDetail.vue)
- 文章内容白色卡片 + 阴影 + 圆角
- 标题 26px + 绿色编辑按钮带图标
- 日期、地点用 SVG 图标装饰
- 分隔线分割元信息与正文
- 引用块圆角 + 绿色左边框 + 浅绿背景
- 404 页面居中卡片
- 页面淡入动画

**6. 写记录页 & 地图页同步设计系统**
- 统一使用 CSS 变量替换硬编码颜色值
- 输入框 focus 状态统一绿色光晕
- 按钮 hover 状态统一阴影效果
- 发布、取消按钮样式优化
- 图片尺寸选择弹窗加入毛玻璃背景 + 弹入动画

---

## 2026-05-13 第4次：搭建后端（Phase 1）— FastAPI + SQLite

### 改动内容

**1. 后端项目结构搭建** [backend/](backend/)
- 创建 `backend/` 目录，包含完整的 FastAPI 项目
- 使用 Python venv 管理虚拟环境，`requirements.txt` 声明依赖
- 依赖：fastapi, uvicorn, sqlalchemy, python-jose, passlib, bcrypt

**2. 数据模型** [backend/models.py](backend/models.py)
- **users 表**：id, username（唯一）, hashed_password, nickname, created_at
- **records 表**：id, user_id（外键）, title, content（Markdown 正文）, record_date, location, lat, lng, created_at, updated_at

**3. 用户认证** [backend/auth.py](backend/auth.py) + [routers/auth.py](backend/routers/auth.py)
- JWT 令牌认证（HS256，24 小时过期）
- 密码使用 bcrypt 哈希存储
- 注册 `POST /api/auth/register`、登录 `POST /api/auth/login`
- 接口统一返回 `{ access_token, token_type, user }`

**4. 记录 CRUD** [backend/routers/records.py](backend/routers/records.py)
- `GET /api/records` — 获取当前用户所有记录（按日期倒序）
- `POST /api/records` — 新建记录（标题、正文、地点、坐标、自定义日期）
- `GET /api/records/{id}` — 获取单条记录
- `PUT /api/records/{id}` — 更新记录（支持部分更新）
- `DELETE /api/records/{id}` — 删除记录
- 所有接口均需 Bearer Token 认证，仅返回当前用户数据

**5. 前端 API 对接**
- 新增 [src/api/index.js](src/api/index.js) — fetch 封装，自动附加 JWT token，401 自动跳登录页
- [stores/records.js](src/stores/records.js) 改为异步 API 调用：
  - CRUD 操作全部调用后端接口
  - 新增 `fetchRecords()` 加载所有记录
  - 新增 `deleteRecord()` 删除记录
  - 保留 `yearTree` / `mapRecords` 计算属性（从本地缓存计算）
- [Login.vue](src/views/Login.vue) 改为真实注册/登录，新增 登录/注册 切换 Tab
- [MainLayout.vue](src/views/MainLayout.vue) 挂载时自动调用 `fetchRecords()`
- [RecordDetail.vue](src/views/RecordDetail.vue) 使用 async `getRecordById` 加载数据

### 启动方式

```bash
# 终端 1：后端
cd backend && source venv/bin/activate && uvicorn main:app --reload --port 8000

# 终端 2：前端（另一个终端）
npm run dev
```

后端 API 文档：http://localhost:8000/docs

---

## 2026-05-13 第5次：补充后端依赖记录

### 后端依赖清单

[backend/requirements.txt](backend/requirements.txt) 完整依赖如下：

| 包名 | 版本 | 用途 |
|------|------|------|
| `fastapi` | 0.104.1 | Web 框架，构建 RESTful API |
| `uvicorn` | 0.24.0 | ASGI 服务器，运行 FastAPI |
| `sqlalchemy` | 2.0.23 | ORM，数据库操作 |
| `python-jose[cryptography]` | 3.3.0 | JWT 令牌生成与验证 |
| `passlib[bcrypt]` | 1.7.4 | 密码哈希（bcrypt 算法） |
| `python-multipart` | 0.0.6 | FastAPI 解析 multipart 表单数据（登录等）|

**注意：** `python-multipart` 是 FastAPI 处理表单数据必需的依赖，之前的日志中未记录。

### 安装方式

```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 启动方式

```bash
# 后端（端口 8000）
cd backend && source venv/bin/activate && uvicorn main:app --reload --port 8000

# 前端（端口 5173，另一个终端）
npm run dev
```

---

## 2026-05-14 00:02 第1次：上线联调修复 + 照片 EXIF GPS 定位

### 地点
- [backend/main.py](backend/main.py) — CORS 配置
- [backend/requirements.txt](backend/requirements.txt) — bcrypt 版本锁定
- [src/views/Login.vue](src/views/Login.vue) — API 路径修正
- [src/stores/records.js](src/stores/records.js) — API 路径修正
- [src/views/RecordEdit.vue](src/views/RecordEdit.vue) — EXIF GPS 定位

### 改动内容

**1. 修复 bcrypt 版本不兼容** [backend/requirements.txt](backend/requirements.txt)
- `bcrypt 5.0.0` 与 `passlib 1.7.4` 不兼容，注册/登录接口返回 500
- 降级锁定 `bcrypt==4.0.1`
- 重建 venv 修复目录名变更导致的路径错误（`My Daily` → `My Diary`）

**2. 修复 CORS 跨域问题** [backend/main.py](backend/main.py)
- 前端 dev server 可能运行在 5173 或 5174 端口
- allow_origins 增加 `localhost:5174` 和 `127.0.0.1:5174`

**3. 修复 API 路径前缀缺失** [src/views/Login.vue](src/views/Login.vue) + [src/stores/records.js](src/stores/records.js)
- 前端请求路径为 `/auth/login`，后端实际路由为 `/api/auth/login`
- 所有 API 调用路径加上 `/api` 前缀
- 涉及：登录、注册、记录 CRUD

**4. 照片 EXIF GPS 自动定位** [src/views/RecordEdit.vue](src/views/RecordEdit.vue)
- 安装 `exifr` 库，选择图片时自动读取内嵌 GPS 坐标
- 读到坐标 → 调用高德逆地理编码 → 自动填入地点输入框和坐标字段
- 无 GPS 信息的图片（截图、网图等）静默忽略

**5. 修复地图 leaflet.markercluster 缺失**
- 安装 `leaflet.markercluster` npm 包
- 重启 dev server 清除 Vite 缓存
